defmodule Assignment5.Math do
  def addition(a,b) do
    IO.puts "The value a is #{a} and b is #{b}"
    a+b
  end

  def subtraction(a,b) do
    IO.puts "The value a is #{a} and b is #{b}"
    a-b
  end

  def multiplication(a,b) do
    IO.puts "The value a is #{a} and b is #{b}"
    a*b
  end

  def division(a,b) do
    IO.puts "The value a is #{a} and b is #{b}"
    a/b
  end

end
